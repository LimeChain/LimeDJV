import React from "react";

const Title = ({ text }) => {

  return (
    <div className="title-wrapper">
      <div>
        <h2>{text}</h2>
      </div>
     
    </div>
  );
};

export default Title;
